# Omni 29-DOF 跑步训练工作总结

> 日期：2026-08-14 ~ 2026-08-15
> 目标：解决跑步训练中手臂不摆动的问题，产出可部署到真机的 ONNX 模型

---

## 一、背景

项目使用 **omni_mimic**（官方 mimic-style 全身控制训练框架）在 AutoDL 上训练 Omni 29-DOF 人形机器人的跑步动作。训练框架基于 Isaac Lab 2.3.2 + Isaac Sim 5.1.0，部署框架为官方 **omni_rl_sdk**（MuJoCo 仿真 + ONNX 推理）。

**核心问题**：原始训练（无手臂惩罚）产出的模型手臂伸展，不摆动。为了"让手臂不伸展"，我们尝试了多种方案，均失败。

---

## 二、尝试过的方案

### 方案 1：两阶段训练（先练走路 → resume 加手臂惩罚）

- **方法**：先用原始配置（无手臂惩罚）训练到收敛，然后 resume 并加入 `joint_pos_l2_penalty`
- **结果**：❌ 崩溃。策略无法在稳固走路基础上学摆臂，走路和手臂同时退化
- **结论**：已放弃

### 方案 2：从零训练 + 手臂惩罚（权重遍历）

从零开始训练，加入 `joint_pos_l2_penalty`（只惩罚手臂 14 个关节），遍历不同权重：

| 权重 | 手臂表现 | 走路表现 | 结论 |
|------|---------|---------|------|
| w0.5 | 下垂 | 原地踏步 |  |
| w1.0 | 下垂 | 原地踏步 | ❌ |
| w2.0 | 下垂 | 原地踏步 | ❌ |
| w5.0 | 微小摆动（不可见） | 原地踏步 | ❌ |

**w5.0 详细训练过程**：
- 0~1000 步：早期崩溃阶段（episode ~1-7），正常自适应
- 1000~2000 步：走路恢复（episode 80-120），手臂误差从 1.65 降到 1.45
- 2000~3000 步：**完全走平**。`error_joint_pos` 稳定在 1.47，`joint_pos_tracking` 稳定在 -0.11，`jt_slope` 趋近于零
- 视频验证：手臂仍然下垂，肉眼看不出摆动

**w5.0 最终指标（3009 步）**：
- mean_reward: 1.526（上升放缓）
- mean_episode_length: ~85（震荡 60-100）
- error_joint_pos: 1.47（完全走平）
- joint_pos_tracking: -0.11（完全走平）
- error_body_pos: 2.5（震荡）
- error_body_lin_vel: 0.83（稳定低位）

### 方案 3：原始配置（无手臂惩罚）

- **方法**：不加入任何手臂惩罚，使用原始奖励配置
- **结果**：✅ 走路正常，但手臂伸展
- **模型**：`2026-08-13_09-15-45/model_8000.pt`（8000 步收敛）
- **这是目前唯一能正常走路的模型**

---

## 三、关键发现

### 3.1 弱 L2 惩罚的时间平均最小化

**根因分析**：弱 L2 惩罚（任何非零权重）被策略通过"把手臂钉在参考动作的时间平均位置（≈下垂）"来最小化，而非真正跟踪摆动。这是数学上的局部最优——固定不动的均方误差低于动态跟踪的均方误差。

### 3.2 手臂惩罚与走路奖励的冲突

跑步时手臂摆动与腿部协调（手臂前摆对应腿后蹬）。如果手臂惩罚强迫手臂跟踪参考动作，但策略还没学会"手臂-腿部协调"，就会破坏走路稳定性。策略选择折中：手臂不动（减少惩罚）+ 走路凑合（勉强维持）。

### 3.3 所有路线均失败的总结

| 方案 | 手臂 | 走路 | 结论 |
|------|------|------|------|
| 无惩罚（原始） | 伸展 | **正常跑步** ✅ | 唯一走路正常的 |
| 两阶段（resume） | 崩溃 | 崩溃 | ❌ |
| 从零 + w0.5~w5.0 | 下垂/微小摆动 | 原地踏步 | ❌ |

---

## 四、ONNX 部署问题

### 4.1 现象

- 跑步 ONNX 在 MuJoCo 仿真中**躺在地上抽搐**，没有任何跑步动作
- 跳高 ONNX 在 MuJoCo 仿真中**能跳起来**（表现尚可）
- 两个 ONNX 都是 omni_mimic 训练的

### 4.2 已排查的信息

- ONNX 元数据中 `action_scale = 0.5`（所有 29 个关节）
- SDK 的 `BasePolicy` 默认 `action_scale = 1.0`
- ONNX 导出代码（`exporter.py`）已将归一化参数打包在 ONNX 内部
- ONNX 有两个输入：`obs`（529 维）和 `time_step`
- ONNX 内部嵌入了参考动作数据（joint_pos, joint_vel, body_quat_w），通过 `time_step` 索引

### 4.3 可能原因（待验证）

1. **action_scale 不匹配**：SDK 默认 scale=1.0，ONNX 期望 scale=0.5，动作被放大 2 倍
2. **obs 构建方式不同**：omni_mimic ONNX 的参考动作嵌入在模型内部，SDK 的 `HighDynamicPolicy` 从外部 NPZ 加载，两者 obs 构建逻辑可能不一致
3. **跳高容错大**：跳高动作幅度大，即使参数有偏差也能勉强工作；跑步需要精确协调，偏差导致立即崩溃

### 4.4 当前状态

- **未解决**。需要项目组在 MuJoCo 端进一步排查
- 我们本地没有 MuJoCo 仿真环境，无法验证

---

## 五、当前可用资产

| 资产 | 位置 | 状态 |
|------|------|------|
| 原始跑步模型 | `logs/rsl_rl/omni_flat/2026-08-13_09-15-45/model_8000.pt` | ✅ 走路正常，手臂伸展 |
| 跑步 ONNX | `2026-08-13_09-15-45_8000.onnx` | ⚠️ MuJoCo 部署失败 |
| 跳高 ONNX | （项目组持有） | ✅ MuJoCo 能跳 |
| 29 个重定向 NPZ 文件 | `retargeted/` 目录 | ✅ 可用 |
| 自动监控脚本 | `omni_mimic/scripts/monitor_training.py` | ✅ 可用 |

---

## 六、面临的困境

1. **手臂摆动问题无解**：穷举了所有路线（两阶段、从零+各种权重），都走不通。任何手臂惩罚都会破坏走路，且即使 w5.0 也产生不了可见的摆臂。

2. **ONNX 部署不通**：跑步 ONNX 在 MuJoCo 中抽搐，无法部署到真机。根因未定位。

3. **时间压力**：机器人大赛在即，需要能跑的 ONNX 部署到真机。

---

## 七、待解决的问题和决策

### 7.1 ONNX 部署（最高优先级）

- [ ] 让项目组确认 SDK 配置中 `action.scale` 是否为 0.5（匹配 ONNX 元数据）
- [ ] 确认 SDK 部署时使用的 Policy 类是否正确（应该是能处理 omni_mimic ONNX 的类，不是 `MXPolicy`）
- [ ] 确认 obs 构建逻辑是否与 omni_mimic 训练时一致
- [ ] 如果上述都正确仍失败，需要官方团队提供 omni_mimic ONNX 的 SDK 适配代码

### 7.2 手臂摆动问题（可搁置）

- [ ] 接受"手臂伸展 + 走路正常"作为当前最优解
- [ ] 或向官方团队咨询：omni_mimic 框架下如何实现手臂摆动而不破坏走路
- [ ] 或考虑修改参考动作数据（retarget 阶段调整手臂轨迹）

### 7.3 其他动作训练

- [ ] 跨栏 02（下一个动作，平地环境，与跑步迁移性最高）
- [ ] 跳高（已有能跳的 ONNX，需确认 MuJoCo 部署稳定性）
- [ ] 楼梯、翻箱子等（需要地形配置，后续再议）

### 7.4 关键决策

**是否需要官方团队介入？**

omni_mimic 是官方提供的训练框架，omni_rl_sdk 是官方提供的部署框架。如果两者产出的 ONNX 无法互通，这可能是框架间的适配问题，需要官方团队提供：
1. omni_mimic ONNX 在 SDK 中的正确部署方式
2. 或提供 omni_mimic 对应的 SDK Policy 类实现

---

## 八、经验教训

1. **不要过早干预训练**：早期崩溃（episode ~1-7）是自适应探索，不是失败。500-1000 步内会恢复。
2. **多指标交叉验证**：不能只看 reward，必须同时看 episode_length、error 指标、entropy。
3. **视频验证不可替代**：指标改善 ≠ 行为改善。w5.0 的 `joint_pos_tracking` 从 -0.05 降到 -0.11，看起来是进步，但视频证明肉眼不可见。
4. **弱 L2 惩罚的陷阱**：弱惩罚被策略通过"时间平均最小化"来应付，产生"下垂"而非"摆动"。
5. **改权重必须从零训练**：resume 会导致价值网络尺度失配 + 继承错误先验。
6. **应该更早要求截 Episode_Reward 和 Metrics 页面**：等到 5000 步才看完整 TensorBoard 太晚了，早期就应该监控。
